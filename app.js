const argv = require("./config/yargs").argv;
const colors = require("colors");

let dato;

const menu = () => {

    let comando = argv._[0];
    switch (comando) {
        case mostrar:

            break;

        default:
            break;
    }

};